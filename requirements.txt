torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
ultralytics>=8.0.0
mlflow>=2.0.0
tensorflow>=2.15.0
flask>=2.3.0
flask-cors>=4.0.0
pillow>=9.0.0
numpy>=1.23.0,<2.0.0
pyyaml>=6.0
jupyter>=1.0.0
notebook>=6.5.0
ipykernel>=6.20.0
requests>=2.31.0
kagglehub>=0.1.0
#
# Para GPU (NVIDIA CUDA):
#   PyTorch instalará automáticamente la versión con CUDA
#   Para forzar versión GPU: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   CUDA 11.8 o 12.1 recomendado
#
# Para desarrollo:
#   pip install black flake8 pytest  # Herramientas de desarrollo
#
# Verificar instalación GPU:
#   python -c "import torch; print('CUDA disponible:', torch.cuda.is_available())"
#   python -c "import torch; print('GPU:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'No detectada')"
#   python -c "import tensorflow as tf; print(tf.config.list_physical_devices('GPU'))"
#
# ===================================================================
